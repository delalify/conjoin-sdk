{
  "name": "spec",
  "targets": {
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash scripts/validate.sh",
        "cwd": "{projectRoot}"
      }
    }
  }
}
